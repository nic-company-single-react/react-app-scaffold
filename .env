# .env.local 예시
# Host 앱 포트
PORT=5173 # 해당하는 포트로 적용

# Vite Base URL (default: /)
VITE_BASE_URL=/

# Vite Router Base Name (default: /)
VITE_ROUTER_BASENAME=/

# JWT를 이용한 access token 저장 이름 설정(미사용 시 공백처리)
VITE_LOCALSTORAGE_TOKEN_NAME=access_token

# 테마(라이트/다크) 저장 localStorage 키 이름 (index.html FOUC 스크립트와 공유하는 단일 소스)
VITE_THEME_STORAGE_KEY=theme

# 초기 스플래시(흰 화면 방지) 설정 — index.html의 #app-splash가 사용. 사이트별로 이 값만 수정한다.
VITE_SPLASH_BG=#ffffff
VITE_SPLASH_BG_DARK=#0b0f19
VITE_SPLASH_ACCENT=#4f46e5
# public/ 안의 로고 파일명 (favicon과 동일하게 logo.ico 재활용)
VITE_SPLASH_LOGO=logo.ico

# local server url
VITE_SERVER_URL=http://localhost:4000

# API 서버 기본 URL (Vite dev proxy: /api → localhost:4000)
# 임시 api 호출 url (https://jsonplaceholder.typicode.com)
VITE_API_BASE_URL=https://jsonplaceholder.typicode.com
