// ★ 활성 레이아웃 선택 지점 (단일 스위치)
// 새 사이트는 ./default 를 복제한 폴더(예: ./acme)를 만든 뒤 아래 경로만 교체하면
// 사이트 전체 레이아웃이 통째로 바뀝니다. (스타일은 assets/styles/layout + app.css 에서 교체)
export { default as RootLayout } from './default';
