// 아이콘 컴포넌트 추가 시 이 파일만 수정
// 각 아이콘은 export default 이므로 `export *`가 아니라 이름을 붙여 재export한다.
export { default as GithubMark } from './GithubMark';

// 추후 다른 아이콘 컴포넌트 추가 시
//export { default as StorybookMark } from './StorybookMark';
